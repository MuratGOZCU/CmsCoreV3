@model IEnumerable<GalleryItem>
@inject CustomLocalizer Localizer
@inject AppTenant Tenant
<div class="gallery-widget widget">
    <div class="title-widget">@Localizer["Galeri"]</div>
    <div class="content-widget">
        <div class="gallery-list">
            @foreach (GalleryItem item in Model)
            {
                <a href="javascript:void(0)" class="thumb"><img src="@item.Photo" alt="@item.Title" class="img-responsive" /></a>
            }

        </div>
        <div class="clearfix"></div>
        <a href="@(Tenant.RequireSSL?"https":"http")://@Tenant.Hostname/@ViewData["culture"]/galeri" class="view-more">@Localizer["Daha Fazla Fotoğraf"]&nbsp;<i class="fa fa-angle-double-right mls"></i></a>
    </div>
</div>